---
apiVersion: v1
kind: Service
metadata:
  name: "{{ meta.name }}-web-svc"
  namespace: "{{ project_name }}"
  labels:
    app: pulp-web
spec:
  selector:
    app: pulp-web
  ports:
    - protocol: TCP
      targetPort: 80
      name: pulp-web-80
      port: 80
      nodePort: 80
  type: NodePort
