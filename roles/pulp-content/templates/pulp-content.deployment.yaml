---
apiVersion: v1
kind: Deployment
metadata:
  name: pulp-content
  namespace: "{{ project_name }}"
  labels:
    app: pulp-content
spec:
  replicas: {{ pulp_content.replicas }}
  selector:
    matchLabels:
      app: pulp-content
  template:
    metadata:
      labels:
        app: pulp-content
    spec:
      #serviceAccountName: "{{ project_name }}-anyuid"
      volumes:
        - name: pulp-server
          configMap:
            name: pulp-server
            items:
              - path: settings.py
                key: settings.py
      containers:
        - name: pulp-content
          image: "{{ registry }}/pulp-content:{{ version }}"
          imagePullPolicy: Always
          ports:
            - protocol: TCP
              containerPort: 24816
          volumeMounts:
            - name: pulp-server
              mountPath: "/etc/pulp/"
